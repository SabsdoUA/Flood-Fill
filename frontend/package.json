{
  "name": "flood-fill-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "clean:cache": "rimraf node_modules/.vite",
    "prebuild": "npm run clean && npm run clean:cache"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/react": "^18.3.20",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "rimraf": "^6.1.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.3"
  }
}
